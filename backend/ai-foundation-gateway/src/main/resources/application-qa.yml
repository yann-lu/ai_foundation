spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_foundation?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&rewriteBatchedStatements=true
    username: ai_app
    password: ${DB_PASSWORD:AiApp@2026}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3000ms
  ai:
    model:
      chat: openai
      embedding: openai
    openai:
      api-key: sk-MNHC5WFsuXTSNOwdq40sP3i5JsA7422hMNuP9EnaZPFdfcKc
      #base-url: http://localhost:11434
      base-url: https://api.moonshot.cn
      chat:
        options:
          model: kimi-k2.6
          extra-body:
            thinking:
              type: disabled
      embedding:
        options:
          model: bge-m3

logging:
  level:
    com.ai.foundation: DEBUG

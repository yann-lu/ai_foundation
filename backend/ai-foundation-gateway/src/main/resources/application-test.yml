spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_foundation?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ai_app
    password: ${DB_PASSWORD:AiApp@2026}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:sk-placeholder-not-used}
      base-url: http://localhost:8081

logging:
  level:
    com.ai.foundation: DEBUG

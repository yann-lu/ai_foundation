<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.ai.foundation</groupId>
        <artifactId>ai-foundation-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <artifactId>ai-foundation-gateway</artifactId>
    <name>ai-foundation-gateway</name>
    <description>Web 网关层：Controller、Filter、配置、启动入口</description>

    <dependencies>
        <dependency>
            <groupId>com.ai.foundation</groupId>
            <artifactId>ai-foundation-mediator</artifactId>
        </dependency>
        <dependency>
            <groupId>com.ai.foundation</groupId>
            <artifactId>ai-foundation-biz</artifactId>
        </dependency>
        <dependency>
            <groupId>com.ai.foundation</groupId>
            <artifactId>ai-foundation-dal</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.session</groupId>
            <artifactId>spring-session-data-redis</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.ai.foundation.gateway.GatewayApplication</mainClass>
                    <!--
                      解决 macOS 上 Netty DNS resolver + Java 25 restricted method 的兼容问题：
                      /enable-native-access=ALL-UNNAMED  允许 Netty 调 native DNS lib
                      -Dio.netty.resolver.dns.default.server.addresses  强制 Netty 用本地 DNS + Google 公共 DNS 作为 fallback
                      没有这两条，所有出站 HTTP（Spring AI 调 LLM、ApiToolExecutor 调外部 API）都会卡在 UnknownHostException
                    -->
                    <jvmArguments>--enable-native-access=ALL-UNNAMED -Dio.netty.resolver.dns.default.server.addresses=127.0.0.1,8.8.8.8</jvmArguments>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
